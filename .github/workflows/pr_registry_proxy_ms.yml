name: Code Review - Registry Proxy
on:
  workflow_dispatch:

  pull_request:
    branches:
      - main
      - releases/**
    types:
      - opened
      - edited
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - "apps/registry-proxy/**"
      - ".github/workflows/pr_registry_proxy_ms.yml"
      - ".github/workflows/call_code_review.yml"

jobs:
  code_review:
    uses: ./.github/workflows/call_code_review.yml
    name: "Code Review"
    permissions:
      contents: read
      pull-requests: write
      packages: read
    secrets: inherit
    if: github.base_ref == 'main' && github.event_name == 'pull_request'
    with:
      pr_number: ${{ github.event.pull_request.number }}
      source_branch: ${{ github.head_ref }}
      target_branch: ${{ github.base_ref }}
      sonar_key: "pagopa_selfcare-ms-party-registry-proxy"
      module: "registry-proxy"
