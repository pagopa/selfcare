name: Core Code Review Infra DX

on:
  pull_request:
    branches:
      - main
      - develop
    types:
      - opened
      - edited
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - ./infra/core/**
      - ./.github/workflows/call_code_review_infra.yml
      - ./.github/workflows/pr_core_infra.yml

  workflow_dispatch:
    inputs:
      env:
        required: true
        type: choice
        description: 'Select the Environment'
        default: dev
        options:
          - dev
          - uat
          - prod
      domain:
        required: true
        type: choice
        description: 'Select the Domain'
        default: ar
        options:
          - ar
          - pnpg

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: read
  pull-requests: write
  id-token: write


jobs:
  plan:
    uses: pagopa/dx/.github/workflows/infra_plan.yaml@main
    name: "${{ inputs.env }} ${{ inputs.domain }} Selfcare Infra Core Release"
    secrets: inherit
    with:
      environment: ${{ inputs.env }}-${{ inputs.domain }}
      base_path: infra/core
      use_private_agent: true
      override_github_environment: ${{ inputs.env }}
      use_labels: true
      override_labels: ${{ inputs.env }}  
