name: Release App
run-name: Release ${{ inputs.app }} (${{ inputs.env }} - ${{ inputs.target }})

on:
  workflow_dispatch:
    inputs:
      env:
        type: choice
        description: Environment
        options:
          - dev
          - uat
          - prod
        required: true
      target:
        type: choice
        description: Target type
        options:
          - ar
          - pnpg
        required: true
        default: ar
      app:
        type: choice
        description: App name
        options:
          - auth
          - iam
          - delegation-cdc
          - document-ms
          - institution-ms
          - institution-send-mail-scheduler
          - onboarding-bff
          - onboarding-cdc
          - onboarding-ms
          - product
          - product-cdc
          - registry-proxy
          - registry-proxy-runner
          - user-cdc
          - user-group-cdc
          - user-group-ms
          - user-ms
          - webhook
          - dashboard-bff
        required: true
        default: auth

jobs:
  release:
    uses: ./.github/workflows/call_release_app.yml
    name: Call Release App
    secrets: inherit
    with:
      env: ${{ inputs.env }}
      target: ${{ inputs.target }}
      app: ${{ inputs.app }}
      APP_PATH: apps/${{ inputs.app }}
      APPS_POM: apps/pom.xml
      POM: pom.xml
